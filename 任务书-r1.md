# LinguaCraft 前端实现任务书 v6

> 本任务书是**纯用户前端**任务书，是三步走的第一步。
> 第二步：后台管理端。第三步：后端 Spring Boot + Spring AI。
> 本任务书不含后台管理端页面，不含后端实现细节。API 约定定位为**前端 mock 用的契约**，采用 MSW 作为 mock 层。
> v6 相对 v5 的修订清单见文末附录。

---

## 0. 项目概述

**项目名**：LinguaCraft
**项目根目录**：`LinguaCraft`（不建子目录，`package.json` 的 `name` 用 `linguacraft`）
**定位**：广东专插本公共英语备考工具
**核心价值**：以真题题型为训练单元，帮助考生针对性提分
**目标用户**：中文母语的广东专插本考生
**终端**：桌面 / 平板 / 手机三端响应式，一套代码
**考试信息**：总分 100 分，120 分钟，无听力

**题型与分值**

| 题型 | 题量 | 分值 |
|---|---|---|
| 阅读理解 | 15题 | 30分 |
| 短文造句匹配（五选五） | 5题 | 10分 |
| 完形填空 | 15题 | 30分 |
| 语法填空 | 10题 | 15分 |
| 写作 | 1篇 | 15分 |

**主题**：亮色为亮蓝色调，暗色为暗蓝色调。
**批改方式**：AI 批改 / 人工批改，用户在设置页选择，默认 AI 批改。
**Mock 方案**：MSW（Mock Service Worker）。

---

## 1. 技术栈

- Vue 3（`<script setup>` + TypeScript）
- Vite
- Vue Router 4
- Pinia
- Naive UI
- VueUse
- ECharts
- markdown-it + highlight.js
- vite-plugin-pwa
- **msw**（mock）
- ESLint + Prettier

---

## 2. 核心设计原则

1. **刷题是主线**：一级导航第一位是"刷题"，首页突出备考倒计时和题型得分率
2. **题型是主导航**：5 种题型是二级菜单主体
3. **来源是筛选维度**：真题 / 模拟题 / AI出题 是每个题型页顶部的切换器，**不是导航层级，不是独立页面**
4. **"模拟题"= 老师出的模拟卷**，只是题目来源之一，学生端只做展示和答题
5. **AI出题无独立页面、无参数面板**：点即生成，loading 后直接答题，后端 Spring AI 实现
6. **批改两条路径**：AI 批改走 Spring AI；非 AI 改走数据库比对，无答案转人工，作文一律人工
7. **答案不下发**：所有 GET 题目接口不得下发答案、解析相关字段，只在 submit 响应中下发
8. **沉浸模式**：所有答题页进入沉浸，隐藏导航，只留练习头部
9. **三端共用逻辑**：路由、业务逻辑、组件共用，只在布局壳和交互细节上分支
10. **双主题**：亮色亮蓝、暗色暗蓝，全站跟随切换
11. **五选五统一点选**：三端都不拖拽，点击配对
12. **MVP 范围**：仅用户前端；无"作业"概念；无后台管理端；无后端实现

---

## 3. 目录结构

```text
LinguaCraft/
├─ public/
│  ├─ favicon.ico
│  ├─ manifest.webmanifest
│  ├─ mockServiceWorker.js    # 由 npx msw init public/ 生成，勿手写
│  └─ hljs/
│     ├─ github.min.css
│     └─ github-dark.min.css
├─ src/
│  ├─ main.ts
│  ├─ App.vue
│  ├─ env.d.ts                # 声明 VITE_USE_MOCK 等自定义环境变量类型
│  ├─ api/
│  │  ├─ request.ts
│  │  ├─ auth.ts
│  │  ├─ home.ts
│  │  ├─ vocab.ts
│  │  ├─ grammar.ts
│  │  ├─ practice.ts
│  │  ├─ exam.ts
│  │  └─ user.ts
│  ├─ assets/
│  │  ├─ logo.svg
│  │  └─ styles/
│  │     ├─ variables.css
│  │     ├─ reset.css
│  │     └─ global.css
│  ├─ components/
│  │  ├─ common/
│  │  │  ├─ AppLogo.vue
│  │  │  ├─ PageHeader.vue
│  │  │  ├─ StatCard.vue
│  │  │  ├─ EmptyState.vue
│  │  │  ├─ LoadingState.vue
│  │  │  ├─ SourceSwitcher.vue
│  │  │  ├─ ThemeToggle.vue
│  │  │  ├─ PendingView.vue
│  │  │  └─ CountdownTimer.vue
│  │  ├─ nav/
│  │  │  ├─ TopNav.vue
│  │  │  ├─ SideNav.vue
│  │  │  ├─ NavDrawer.vue
│  │  │  ├─ MobileTopBar.vue
│  │  │  ├─ MobileSubTabs.vue
│  │  │  └─ MobileTabBar.vue
│  │  ├─ vocab/
│  │  │  ├─ WordCard.vue
│  │  │  ├─ WordActionBar.vue
│  │  │  ├─ WordProgressAside.vue
│  │  │  └─ WordListTable.vue
│  │  ├─ grammar/
│  │  │  ├─ GrammarTree.vue
│  │  │  └─ GrammarContent.vue
│  │  ├─ practice/
│  │  │  ├─ reading/
│  │  │  │  ├─ PassageRenderer.vue
│  │  │  │  ├─ QuestionList.vue
│  │  │  │  ├─ QuestionItem.vue
│  │  │  │  └─ AnswerSheet.vue
│  │  │  ├─ matching/
│  │  │  │  ├─ OptionPool.vue
│  │  │  │  ├─ OptionCard.vue
│  │  │  │  ├─ BlankSlot.vue
│  │  │  │  └─ OptionPickerDrawer.vue
│  │  │  ├─ cloze/
│  │  │  │  ├─ ClozePassage.vue
│  │  │  │  └─ ClozeQuestion.vue
│  │  │  ├─ grammar-fill/
│  │  │  │  ├─ FillPassage.vue
│  │  │  │  └─ FillBlank.vue
│  │  │  ├─ writing/
│  │  │  │  ├─ WritingPrompt.vue
│  │  │  │  └─ WritingEditor.vue
│  │  │  └─ shared/
│  │  │     ├─ PracticeListPage.vue
│  │  │     └─ PracticeSourceBar.vue
│  │  └─ exam/
│  │     ├─ ExamPaper.vue
│  │     └─ ExamReport.vue
│  ├─ composables/
│  │  ├─ useDevice.ts
│  │  ├─ useImmersive.ts
│  │  ├─ useSwipe.ts
│  │  ├─ useCountdown.ts
│  │  ├─ useHighlightTheme.ts
│  │  └─ usePlaceholder.ts
│  ├─ layouts/
│  │  ├─ AppLayout.vue
│  │  └─ BlankLayout.vue
│  ├─ mocks/
│  │  ├─ browser.ts
│  │  ├─ handlers/
│  │  │  ├─ auth.ts
│  │  │  ├─ home.ts
│  │  │  ├─ practice.ts
│  │  │  ├─ exam.ts
│  │  │  ├─ vocab.ts
│  │  │  ├─ grammar.ts
│  │  │  └─ user.ts
│  │  ├─ utils/
│  │  │  └─ stripAnswer.ts   # Full → ForAnswer 答案过滤工具
│  │  └─ data/
│  │     ├─ reading.ts
│  │     ├─ matching.ts
│  │     ├─ cloze.ts
│  │     ├─ grammar-fill.ts
│  │     ├─ writing.ts
│  │     ├─ exam.ts
│  │     ├─ words.ts
│  │     └─ grammar-tree.ts
│  ├─ router/
│  │  ├─ index.ts
│  │  ├─ routes.ts
│  │  └─ nav.ts
│  ├─ stores/
│  │  ├─ user.ts
│  │  ├─ vocab.ts
│  │  ├─ grammar.ts
│  │  ├─ practice.ts
│  │  ├─ exam.ts
│  │  └─ app.ts
│  ├─ types/
│  │  ├─ api.d.ts
│  │  ├─ practice.d.ts
│  │  ├─ exam.d.ts
│  │  ├─ vocab.d.ts
│  │  ├─ grammar.d.ts
│  │  └─ user.d.ts
│  ├─ utils/
│  │  ├─ storage.ts
│  │  ├─ format.ts
│  │  └─ shuffle.ts
│  └─ views/
│     ├─ Login.vue
│     ├─ Register.vue
│     ├─ ForgotPassword.vue
│     ├─ ResetPassword.vue
│     ├─ Dashboard.vue
│     ├─ learn/
│     │  ├─ Vocab.vue
│     │  └─ Grammar.vue
│     ├─ practice/
│     │  ├─ Reading.vue
│     │  ├─ Matching.vue
│     │  ├─ Cloze.vue
│     │  ├─ GrammarFill.vue
│     │  └─ Writing.vue
│     ├─ exam/
│     │  ├─ ExamList.vue
│     │  └─ ExamDetail.vue
│     └─ user/
│        ├─ UserCenter.vue
│        ├─ Stats.vue
│        ├─ Submissions.vue
│        ├─ Books.vue
│        ├─ Mistakes.vue
│        ├─ Favorites.vue
│        └─ Settings.vue
├─ index.html
├─ package.json
├─ tsconfig.json
├─ vite.config.ts
├─ .eslintrc.cjs
├─ .prettierrc
├─ .env.development
└─ README.md
```

---

## 4. 路由设计

```ts
export const routes = [
  { path: '/login', component: () => import('@/views/Login.vue'), meta: { layout: 'blank' } },
  { path: '/register', component: () => import('@/views/Register.vue'), meta: { layout: 'blank' } },
  { path: '/forgot-password', component: () => import('@/views/ForgotPassword.vue'), meta: { layout: 'blank' } },
  { path: '/reset-password', component: () => import('@/views/ResetPassword.vue'), meta: { layout: 'blank' } },
  {
    path: '/',
    component: () => import('@/layouts/AppLayout.vue'),
    redirect: '/dashboard',
    children: [
      { path: 'dashboard', component: () => import('@/views/Dashboard.vue'),
        meta: { title: '首页', section: 'home' } },

      { path: 'practice/reading', component: () => import('@/views/practice/Reading.vue'),
        meta: { title: '阅读理解', section: 'practice', immersive: true } },
      { path: 'practice/matching', component: () => import('@/views/practice/Matching.vue'),
        meta: { title: '五选五', section: 'practice', immersive: true } },
      { path: 'practice/cloze', component: () => import('@/views/practice/Cloze.vue'),
        meta: { title: '完形填空', section: 'practice', immersive: true } },
      { path: 'practice/grammar-fill', component: () => import('@/views/practice/GrammarFill.vue'),
        meta: { title: '语法填空', section: 'practice', immersive: true } },
      { path: 'practice/writing', component: () => import('@/views/practice/Writing.vue'),
        meta: { title: '作文', section: 'practice', immersive: true } },

      { path: 'exam', component: () => import('@/views/exam/ExamList.vue'),
        meta: { title: '真题模考', section: 'practice' } },
      { path: 'exam/:id', component: () => import('@/views/exam/ExamDetail.vue'),
        meta: { title: '模考', section: 'practice', immersive: true } },

      { path: 'learn/vocab', component: () => import('@/views/learn/Vocab.vue'),
        meta: { title: '背单词', section: 'learn' } },
      { path: 'learn/grammar', component: () => import('@/views/learn/Grammar.vue'),
        meta: { title: '学语法', section: 'learn' } },

      { path: 'user', redirect: '/user/stats', meta: { section: 'user' } },
      { path: 'user/stats', component: () => import('@/views/user/Stats.vue'),
        meta: { title: '学习统计', section: 'user' } },
      { path: 'user/submissions', component: () => import('@/views/user/Submissions.vue'),
        meta: { title: '我的提交', section: 'user' } },
      { path: 'user/books', component: () => import('@/views/user/Books.vue'),
        meta: { title: '我的词书', section: 'user' } },
      { path: 'user/mistakes', component: () => import('@/views/user/Mistakes.vue'),
        meta: { title: '错题本', section: 'user' } },
      { path: 'user/favorites', component: () => import('@/views/user/Favorites.vue'),
        meta: { title: '收藏', section: 'user' } },
      { path: 'user/settings', component: () => import('@/views/user/Settings.vue'),
        meta: { title: '设置', section: 'user' } },
    ],
  },
  { path: '/:pathMatch(.*)*', redirect: '/' },
]
```

**路由守卫**：只做两件事——未登录跳 `/login`；设置 `document.title`。沉浸模式的 body class 由 `useImmersive` 统一管理。

**重做跳转**：错题本「重做」用 query 传参，不新增动态路由：

```
/practice/reading?id=xxx&from=mistakes
/practice/matching?id=xxx&from=mistakes
/practice/cloze?id=xxx&from=mistakes
/practice/grammar-fill?id=xxx&from=mistakes
/practice/writing?id=xxx&from=mistakes
```

题型页读取 `route.query.id`，有则直接进入该题答题，无则走来源列表流程。

---

## 5. 双主题设计

### 5.1 配色变量

```css
:root {
  /* 亮色：亮蓝色调 */
  --lc-primary: #3B82F6;
  --lc-primary-hover: #60A5FA;
  --lc-primary-pressed: #2563EB;
  --lc-primary-soft: #EFF6FF;
  --lc-success: #18A058;
  --lc-warning: #F0A020;
  --lc-error: #D03050;
  --lc-info: #3B82F6;

  --lc-bg: #F8FAFC;
  --lc-bg-card: #FFFFFF;
  --lc-bg-elevated: #FFFFFF;
  --lc-bg-hover: #F1F5F9;
  --lc-border: #E2E8F0;
  --lc-text-1: #0F172A;
  --lc-text-2: #475569;
  --lc-text-3: #94A3B8;

  --lc-radius: 10px;
  --lc-radius-lg: 14px;
  --lc-shadow-sm: 0 1px 2px rgba(15,23,42,.04);
  --lc-shadow: 0 4px 16px rgba(15,23,42,.06);
}

html.dark {
  /* 暗色：暗蓝色调 */
  --lc-primary: #60A5FA;
  --lc-primary-hover: #93C5FD;
  --lc-primary-pressed: #3B82F6;
  --lc-primary-soft: #1E293B;
  --lc-success: #34D399;
  --lc-warning: #FBBF24;
  --lc-error: #F87171;
  --lc-info: #60A5FA;

  --lc-bg: #0B1120;
  --lc-bg-card: #111827;
  --lc-bg-elevated: #1E293B;
  --lc-bg-hover: #1E293B;
  --lc-border: #1E293B;
  --lc-text-1: #F1F5F9;
  --lc-text-2: #CBD5E1;
  --lc-text-3: #64748B;

  --lc-shadow-sm: 0 1px 2px rgba(0,0,0,.4);
  --lc-shadow: 0 4px 16px rgba(0,0,0,.5);
}
```

### 5.2 Naive UI 主题覆盖

```ts
export const lightOverrides = {
  common: {
    primaryColor: '#3B82F6', primaryColorHover: '#60A5FA',
    primaryColorPressed: '#2563EB', primaryColorSuppl: '#60A5FA',
    borderRadius: '10px', fontSize: '14px',
    bodyColor: '#F8FAFC', cardColor: '#FFFFFF',
    modalColor: '#FFFFFF', popoverColor: '#FFFFFF',
    borderColor: '#E2E8F0', textColorBase: '#0F172A',
  },
}

export const darkOverrides = {
  common: {
    primaryColor: '#60A5FA', primaryColorHover: '#93C5FD',
    primaryColorPressed: '#3B82F6', primaryColorSuppl: '#93C5FD',
    borderRadius: '10px', fontSize: '14px',
    bodyColor: '#0B1120', cardColor: '#111827',
    modalColor: '#1E293B', popoverColor: '#1E293B',
    borderColor: '#1E293B', textColorBase: '#F1F5F9',
  },
}
```

### 5.3 app store

**主题偏好权威源：localStorage。** 切换只写本地，不调服务端，不做跨设备同步，`/user/settings` 接口也不包含 themeMode 字段。

```ts
export const useAppStore = defineStore('app', () => {
  type ThemeMode = 'light' | 'dark' | 'system'
  const themeMode = ref<ThemeMode>('system')
  const systemPrefersDark = ref(false)
  const siderCollapsed = ref(false)
  const drawerOpen = ref(false)

  const isDark = computed(() => {
    if (themeMode.value === 'system') return systemPrefersDark.value
    return themeMode.value === 'dark'
  })

  function initTheme() {
    const saved = localStorage.getItem('lc-theme') as ThemeMode | null
    if (saved) themeMode.value = saved
    const mql = window.matchMedia('(prefers-color-scheme: dark)')
    systemPrefersDark.value = mql.matches
    const onChange = (e: MediaQueryListEvent) => { systemPrefersDark.value = e.matches }
    if (mql.addEventListener) mql.addEventListener('change', onChange)
    else if (mql.addListener) mql.addListener(onChange)
    applyTheme()
  }

  function setThemeMode(mode: ThemeMode) {
    themeMode.value = mode
    localStorage.setItem('lc-theme', mode)
    applyTheme()
  }

  function toggleTheme() { setThemeMode(isDark.value ? 'light' : 'dark') }

  function applyTheme() {
    document.documentElement.classList.toggle('dark', isDark.value)
    applyHighlightTheme(isDark.value)
  }

  return { themeMode, isDark, siderCollapsed, drawerOpen, initTheme, setThemeMode, toggleTheme }
})
```

`initTheme()` 在 `main.ts` 里调用一次。

### 5.4 highlight.js 双主题（本地打包，不用 CDN）

两套 CSS 放到 `public/hljs/`，`index.html` 里同时引入，用 `disabled` 切换：

```html
<link id="hljs-light" rel="stylesheet" href="/hljs/github.min.css">
<link id="hljs-dark" rel="stylesheet" href="/hljs/github-dark.min.css" disabled>
```

```ts
export function applyHighlightTheme(isDark: boolean) {
  (document.getElementById('hljs-light') as HTMLLinkElement).disabled = isDark
  (document.getElementById('hljs-dark') as HTMLLinkElement).disabled = !isDark
}
```

PWA 预缓存清单需包含这两个文件。


### 5.5 ECharts 适配

```ts
const chartOption = computed(() => ({
  textStyle: { color: appStore.isDark ? '#CBD5E1' : '#475569' },
  xAxis: {
    axisLine: { lineStyle: { color: appStore.isDark ? '#1E293B' : '#E2E8F0' } },
    axisLabel: { color: appStore.isDark ? '#64748B' : '#94A3B8' },
  },
  yAxis: { /* 同上 */ },
}))
```

### 5.6 切换入口

| 位置 | 说明 |
|---|---|
| 桌面 / 平板顶栏 | `ThemeToggle.vue` 太阳/月亮按钮 |
| 手机顶部栏 | 头像下拉菜单"切换主题"项 |
| 个人中心 → 设置 | 三选一：跟随系统 / 亮色 / 暗色 |

### 5.7 特殊页面配色

| 页面 | 亮色 | 暗色 |
|---|---|---|
| 阅读页护眼模式 | `#FAF6E9` | `#1A1814` |
| 批改态正确 | `#18A058` | `#34D399` |
| 批改态错误 | `#D03050` | `#F87171` |

---

## 6. 三端响应式规范

### 6.1 断点

```ts
export function useDevice() {
  const isMobile  = useMediaQuery('(max-width: 767px)')
  const isTablet  = useMediaQuery('(min-width: 768px) and (max-width: 1023px)')
  const isDesktop = useMediaQuery('(min-width: 1024px)')
  return { isMobile, isTablet, isDesktop }
}
```

### 6.2 三端导航形态

| 层级 | 桌面 ≥1024 | 平板 768~1023 | 手机 <768 |
|---|---|---|---|
| 一级导航 | 顶部水平菜单 | 顶部水平菜单（横向滚动） | 底部 Tab |
| 二级导航 | 左侧常驻 200px | 左侧抽屉 240px | 顶部横向 Tab |
| 主内容 padding | 24px | 24px | 16px |
| 主内容 max-width | 1200px 居中 | 全宽 | 全宽 |
| 答题页 | 沉浸 | 沉浸 | 沉浸 + 隐藏底部 Tab |

### 6.3 导航配置

```ts
export const navConfig = [
  {
    key: 'practice', label: '刷题', icon: 'CreateOutline',
    children: [
      { key: '/practice/reading', label: '阅读理解', icon: 'DocumentTextOutline' },
      { key: '/practice/matching', label: '五选五', icon: 'ExtensionPuzzleOutline' },
      { key: '/practice/cloze', label: '完形填空', icon: 'ReaderOutline' },
      { key: '/practice/grammar-fill', label: '语法填空', icon: 'CreateOutline' },
      { key: '/practice/writing', label: '作文', icon: 'PencilOutline' },
      { key: '/exam', label: '真题模考', icon: 'TrophyOutline' },
    ],
  },
  {
    key: 'learn', label: '学习', icon: 'BookOutline',
    children: [
      { key: '/learn/vocab', label: '背单词', icon: 'BookOutline' },
      { key: '/learn/grammar', label: '学语法', icon: 'SchoolOutline' },
    ],
  },
  {
    key: 'user', label: '我的', icon: 'PersonOutline',
    children: [
      { key: '/user/stats', label: '学习统计' },
      { key: '/user/submissions', label: '我的提交' },
      { key: '/user/books', label: '我的词书' },
      { key: '/user/mistakes', label: '错题本' },
      { key: '/user/favorites', label: '收藏' },
      { key: '/user/settings', label: '设置' },
    ],
  },
]
```

**高亮规则**：一级用 `route.meta.section`，二级用 `route.path` 前缀匹配。

### 6.4 AppLayout 骨架

```vue
<template>
  <n-config-provider
    :theme="appStore.isDark ? darkTheme : null"
    :theme-overrides="appStore.isDark ? darkOverrides : lightOverrides"
  >
    <n-message-provider>
      <n-dialog-provider>
        <n-layout position="absolute">
          <n-layout-header v-if="!isMobile && !isImmersive" bordered style="height: 56px">
            <TopNav />
          </n-layout-header>

          <n-layout-sider
            v-if="isDesktop && !isImmersive"
            bordered collapse-mode="width"
            :width="200" :collapsed-width="64"
            :collapsed="appStore.siderCollapsed" show-trigger
          >
            <SideNav :collapsed="appStore.siderCollapsed" />
          </n-layout-sider>

          <n-drawer
            v-if="isTablet"
            v-model:show="appStore.drawerOpen"
            placement="left" :width="240"
          >
            <SideNav :collapsed="false" />
          </n-drawer>

          <n-layout-header v-if="isMobile && !isImmersive" bordered style="height: 48px">
            <MobileTopBar />
          </n-layout-header>

          <MobileSubTabs v-if="isMobile && !isImmersive" />

          <n-layout-content :content-style="contentStyle" :native-scrollbar="false">
            <router-view v-slot="{ Component }">
              <transition name="fade" mode="out-in">
                <component :is="Component" />
              </transition>
            </router-view>
          </n-layout-content>

          <n-layout-footer v-if="isMobile && !isImmersive" class="mobile-tabbar">
            <MobileTabBar />
          </n-layout-footer>
        </n-layout>
      </n-dialog-provider>
    </n-message-provider>
  </n-config-provider>
</template>
```

```ts
const contentStyle = computed(() => ({
  padding: isMobile.value ? '16px' : '24px',
  paddingBottom: isMobile.value
    ? 'calc(56px + env(safe-area-inset-bottom) + 16px)'
    : '24px',
  maxWidth: isDesktop.value ? '1200px' : '100%',
  margin: '0 auto',
}))
```

### 6.5 沉浸模式

```ts
export function useImmersive() {
  const route = useRoute()
  const isImmersive = computed(() => !!route.meta.immersive)
  watchEffect(() => {
    document.body.classList.toggle('immersive', isImmersive.value)
  })
  return { isImmersive }
}
```

---

## 7. 设计规范（非颜色部分）

### 7.1 字体

- 中文：`PingFang SC, Microsoft YaHei, sans-serif`
- 英文：`Inter, -apple-system, sans-serif`
- 正文 14px，行高 1.6
- 标题 h1 24 / h2 20 / h3 16
- 手机正文 15~16px，输入框 16px（防 iOS 缩放）

### 7.2 间距

- 基础单位 4px；常用 8 / 12 / 16 / 24 / 32
- 卡片内边距：桌面 20px，手机 16px
- 卡片间距：桌面 16px，手机 12px

### 7.3 圆角与阴影

- 卡片：`--lc-radius-lg` + `--lc-shadow`
- 按钮 / 输入：`--lc-radius`
- 移动端底部 Tab 用 `border-top`，不用阴影

---

## 8. 占位符规范

**统一为 `[[n]]`**，避免与 Vue 模板插值 `{{ }}` 混淆。

- 五选五：`Reading is one of... [[1]] ... First, [[2]] ...`
- 完形填空：`... who [[1]] from home ...`
- 语法填空：`... who [[1]] (work) ...`
- 阅读：无占位符

解析规则：正则 `/\[\[(\d+)\]\]/g`。

```ts
export function parsePlaceholders(text: string) {
  const regex = /\[\[(\d+)\]\]/g
  const parts: Array<{ type: 'text'; value: string } | { type: 'slot'; id: number }> = []
  let last = 0
  let m: RegExpExecArray | null
  while ((m = regex.exec(text))) {
    if (m.index > 0 && m.index > last) parts.push({ type: 'text', value: text.slice(last, m.index) })
    parts.push({ type: 'slot', id: Number(m[1]) })
    last = m.index + m[0].length
  }
  if (last < text.length) parts.push({ type: 'text', value: text.slice(last) })
  return parts
}
```

---

## 9. 批改方式

### 9.1 总览

```
提交答案
   ├── AI改  → Spring AI 全权处理（判分 + 解析 + 作文评分）
   └── 非AI改
       ├── 选择题（阅读/五选五/完形/语法填空）
       │     ├── 数据库有答案 → 自动比对
       │     └── 数据库无答案 → 转后台，等真人改
       └── 作文 → 转后台，等真人改
```

**"选择题"包含**：阅读理解、五选五、完形填空、语法填空。

### 9.2 用户选择入口

设置页全局偏好：

```
批改方式
  ○ AI 批改（提交后即时出结果）
  ○ 人工批改（稍后出结果）
```

默认 AI 批改。字段：`gradingMethod: 'ai' | 'manual'`，默认 `'ai'`。

### 9.3 前端处理规则

**统一状态**

```ts
type GradingMethod = 'ai' | 'auto' | 'manual'
type GradingStatus = 'graded' | 'partially_graded' | 'pending'
```

| status | 行为 |
|---|---|
| `graded` | 完整批改态：正确绿色、错误红色、展开解析；作文展示评分维度和范文 |
| `partially_graded` | 部分批改：客观题展示批改结果，作文区显示"待批改"（仅整卷模考出现） |
| `pending` | 全屏 pending 占位页，不展示对错和解析，显示 `estimatedTime` |

**提示文案按 status × gradingMethod 组合**：

| status | gradingMethod | 展示文案 |
|---|---|---|
| `graded` / `partially_graded`（客观题部分） | `ai` | "AI 批改完成" |
| `graded` / `partially_graded`（客观题部分） | `auto` | "批改完成" |
| `graded` | `manual` | "人工批改完成" |
| `pending` | `manual` / `ai`（AI 超时降级） | "已提交，等待批改" |

**提交后逻辑**

```ts
async function handleSubmit() {
  const res = await submitPractice(type, id, {
    answers,
    duration,
    gradingMethod: userStore.settings.gradingMethod,
  })

  if (res.status === 'graded') {
    renderResult(res)
  } else if (res.status === 'partially_graded') {
    renderPartialResult(res)
  } else {
    showPendingView(res.estimatedTime)
  }
}
```

**大小写敏感度**：语法填空判分的大小写敏感偏好（`grammarFillCaseSensitive`）是**服务端生效**的设置：前端通过 `PUT /user/settings` 保存，判分由服务端读取执行，submit 请求不携带该参数。

**作文提交**：如果 `gradingMethod === 'manual'`，永远走 pending 态。如果 `gradingMethod === 'ai'` 但 AI 判分超时或失败，后端自动转人工，返回 pending。

---

## 10. 页面详细设计

### 10.1 登录页 `/login`

居中卡片，桌面宽 400px，手机全宽 padding 24px。

内容：Logo + 产品名 + 副标题 + 账号密码表单 + 登录按钮 + 忘记密码/注册链接。

登录成功跳 `/dashboard`。

### 10.2 注册页 `/register`

居中卡片，桌面宽 400px，手机全宽 padding 24px，与登录页同风格。

```
┌──────────────────────────────┐
│  注册 LinguaCraft             │
│                              │
│  [用户名 _________________]  │
│  [邮箱   _________________]  │
│  [昵称   _________________]  │
│  [密码   _________________]  │
│  [确认密码 ________________]  │
│                              │
│  [      注册并登录     ]      │
│                              │
│  已有账号？去登录             │
└──────────────────────────────┘
```

- 用户名：必填，3~20 位字母数字
- 邮箱：必填，用于找回密码
- 昵称：必填
- 密码：必填，≥6 位
- 确认密码：必填，与密码一致

提交调 `POST /auth/register`，响应同 login（返回 token），**注册即登录**，成功后跳 `/dashboard`。

登录页"注册链接"跳 `/register`，注册页"去登录"跳 `/login`。

### 10.3 忘记密码 `/forgot-password`

```
┌──────────────────────────────┐
│  忘记密码                     │
│                              │
│  [邮箱 ___________________]  │
│                              │
│  [    发送重置邮件    ]       │
│                              │
│  返回登录                     │
└──────────────────────────────┘
```

提交后调 `POST /auth/forgot-password`，提示"重置邮件已发送"。

### 10.4 重置密码 `/reset-password?token=xxx`

```
┌──────────────────────────────┐
│  重置密码                     │
│                              │
│  [新密码 _________________]  │
│  [确认密码 ________________] │
│                              │
│  [    确认重置    ]           │
│                              │
│  返回登录                     │
└──────────────────────────────┘
```

提交后调 `POST /auth/reset-password`，成功后跳 `/login`。

> 注：token 放 URL query 会进入浏览器历史与访问日志，MVP 阶段可接受；后端阶段建议改为一次性 code（短期有效）+ 登录后重置，避免 token 泄漏。

### 10.5 首页 `/dashboard`

**桌面**

```
┌──────────────────────────────────────────────────────────┐
│ 下午好，小明 👋                          🌙 主题切换      │
│                                                          │
│         距广东专插本考试还有  128  天                     │
│         ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░  已备考 42%                  │
├──────────────────────────────────────────────────────────┤
│ ⏳ 你有 2 份提交待批改  [查看]                            │
├──────────────────────────┬───────────────────────────────┤
│ 今日任务                  │ 各题型得分率                  │
│ ✅ 阅读理解 2篇（已完成） │ 阅读理解  ▓▓▓▓▓▓▓░░░  72%    │
│ ⬜ 完形填空 1篇           │ 五选五    ▓▓▓▓▓▓▓▓░░  80%    │
│ ⬜ 语法填空 1篇           │ 完形填空  ▓▓▓▓▓░░░░░  55% ←  │
│ ⬜ 背单词 50个            │ 语法填空  ▓▓▓▓▓▓░░░░  62%    │
│ [开始今日学习 →]          │ 作文      ▓▓▓▓▓▓▓▓░░  78%    │
├──────────────────────────┴───────────────────────────────┤
│ [真题模考]  [智能练习]  [我的错题]  [学习统计]            │
└──────────────────────────────────────────────────────────┘
```

**手机**：单列，顺序为 问候 → 倒计时 → 待批改提示（有则显示）→ 今日任务 → 题型得分率 → 快捷入口。

**快捷入口**：
- **真题模考** → `/exam`
- **智能练习** → 调 `POST /practice/smart`，返回 `PracticeItem`，`type` 从 `item.type` 取，直接进入答题
- **我的错题** → `/user/mistakes`
- **学习统计** → `/user/stats`

**待批改提示**：文案"你有 N 份提交待批改"，N > 0 时显示；「查看」跳 `/user/submissions?status=pending`。

---

### 10.6 通用答题页结构

所有答题页顶部栏结构一致，**只有来源切换，没有难度/题材下拉**：

```
┌ ← 退出   [题型名]   [真题|模拟题|AI出题]   计时 XX:XX   [提交] ┐
```

**来源切换器 `SourceSwitcher.vue`**

| 来源 | 点击后 |
|---|---|
| 真题 | 展示真题列表（按年份筛选）→ 选一套 → 答题 |
| 模拟题 | 展示模拟卷列表（按老师/机构筛选）→ 选一套 → 答题 |
| AI出题 | **直接调后端生成**，loading 后进入答题，**无参数面板** |

真题和模拟题的列表页**完全复用同一套组件**。

**AI 出题 loading 态**

```
┌──────────────────────────────────────┐
│              🤖                      │
│         AI 正在为你出题...            │
│         大约需要 10~20 秒             │
│         [取消]                       │
└──────────────────────────────────────┘
```

生成失败给"重试"按钮。

**待批改占位页 `PendingView.vue`**

```
┌──────────────────────────────────────────────────────────┐
│ ← 退出  阅读理解                                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                    📤                                    │
│              已提交，等待批改中                            │
│                                                          │
│           预计 24 小时内出结果                             │
│                                                          │
│           [返回列表]  [刷新状态]                           │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

用户离开后重新进入，已提交但未批改的题目显示"查看结果"按钮，点击后经 `GET /user/submissions/:id` 查询最新状态。

---

### 10.7 阅读理解 `/practice/reading`

**桌面**：左文章 + 右题目

```
┌──────────────────────────────────────────────────────────┐
│ ← 退出  阅读理解  [真题|模拟题|AI出题]      计时  [提交]│
├────────────────────────────────┬─────────────────────────┤
│ 文章区                          │ 题目区                  │
│ 点击单词查词                    │ 1. ...                  │
│ 选中句子收藏                    │ 2. ...                  │
│ [Aa 字号] [🌙 护眼]             │ 答题卡 ● ● ○ ○ ○        │
└────────────────────────────────┴─────────────────────────┘
```

**平板/手机**：上下布局 + Tab 切换「文章 / 题目 N」

**交互**
- 点词 → 桌面 `n-popover`，手机底部 `n-drawer`
- 提交后按 `SubmitResult.status` 进入批改态或 pending 态
- 计时超时自动提交

---

### 10.8 五选五 `/practice/matching`（三端点选，无拖拽）

**核心交互**：支持两种顺序
- **先点空格再点选项**：点击空格 → 高亮 → 点击选项 → 填入
- **先点选项再点空格**：点击选项 → 高亮 → 点击空格 → 填入

**桌面布局**

```
┌──────────────────────────────────────────────────────────┐
│ ← 退出  五选五  [真题|模拟题|AI出题]     计时  [提交]  │
├──────────────────────────────────────────────────────────┤
│ 选项区（点击选择，已用的置灰）                            │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────┐│
│ │ A       │ │ B       │ │ C       │ │ D       │ │ E     ││
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └───────┘│
├──────────────────────────────────────────────────────────┤
│ 文章区                                                    │
│ Reading is one of the most valuable habits...            │
│ ┌──────────────────────┐                                 │
│ │  [[1]] 点击选择选项   │                                 │
│ └──────────────────────┘                                 │
│ First, set a fixed time every day.                       │
│ ┌──────────────────────┐                                 │
│ │  [[2]] 已选 B   ✕    │                                 │
│ └──────────────────────┘                                 │
└──────────────────────────────────────────────────────────┘
```

**手机布局**：选项区横向可滚动，点击空格弹出底部 `n-drawer` 选择面板。

**状态样式**

| 状态 | 空格槽位 | 选项卡片 |
|---|---|---|
| 默认 | 虚线边框 | 正常 |
| 选中（待匹配） | 主色边框 + 浅色背景 | 主色边框 |
| 已填 | 实线边框 + 选项内容 | 置灰，不可再选 |
| 批改正确 | 绿色边框 + ✅ | — |
| 批改错误 | 红色边框 + ❌ | — |

**状态管理**

```ts
const activeBlankId = ref<number | null>(null)
const activeOptionKey = ref<string | null>(null)

function onBlankClick(blankId: number) {
  if (answers[blankId]) { delete answers[blankId]; return }
  if (activeOptionKey.value) {
    answers[blankId] = activeOptionKey.value
    activeOptionKey.value = null
  } else {
    activeBlankId.value = blankId
    if (isMobile.value) openPickerDrawer(blankId)
  }
}

function onOptionClick(key: string) {
  if (isUsed(key)) return
  if (activeBlankId.value) {
    answers[activeBlankId.value] = key
    activeBlankId.value = null
  } else {
    activeOptionKey.value = key
  }
}
```


---

### 10.9 完形填空 `/practice/cloze`

桌面：文章区在上，题目列表在下，答题卡在底部。手机：文章和题目分开 Tab 切换，点击空格弹出底部选择面板。

---

### 10.10 语法填空 `/practice/grammar-fill`

桌面：文章区在上，填空列表在下。手机：点击空格 → 底部弹出输入框。

- 每空有提示词或无提示词
- 输入框 `n-input` `size="large"`
- 提交后逐空显示正确/错误，展开解析
- 大小写敏感度在设置页可配置（默认不敏感），为**服务端判分偏好**：前端保存到 `PUT /user/settings`，判分时由服务端读取生效（见 9.3）

---

### 10.11 作文 `/practice/writing`

**桌面**：左题目 + 右写作区。**手机**：上下布局，字数统计固定底部。

**关键功能**
- 字数实时统计
- 草稿自动保存（localStorage）
- 提交后按 `SubmitResult.status` 进入批改态（展示评分维度和范文）或 pending 态

---

### 10.12 真题模考 `/exam` 和 `/exam/:id`

**列表页 `/exam`**：按年份倒序展示真题套卷。筛选栏 `[2026][2025][2024][2023][更早]`。

**模考页 `/exam/:id`**：整卷答题，顶部 5 个部分跳转，中间答题区，底部答题卡（阅读 1-15、匹配 16-20、完形 21-35、语法 36-45、作文）。

**交卷后**

| status | 行为 |
|---|---|
| `graded` | 完整成绩报告：总分 + 各题型得分 + 薄弱项 + 历年对比 |
| `partially_graded` | 客观题得分先出，作文区显示"待批改" |
| `pending` | pending 占位页 |

成绩报告经 `GET /exam/submission/:submissionId/report` 获取，submissionId 来自交卷响应的 `SubmitResult.submissionId`。

---

### 10.13 背单词 `/learn/vocab`

**桌面**：左单词卡 + 右进度栏

```
┌──────────────────────────────────────────┬───────────────┐
│ 顶部：[词书 ▾] [模式切换]     今日 12/20 │ 今日进度       │
│ 进度条                                    │ 新学 8 复习 4  │
│              abandon                     │ 待复习 36      │
│           /əˈbændən/ 🔊                  │ 已掌握 128     │
│        v. 放弃；抛弃；遗弃                │ [进入复习]     │
│   例：He abandoned his car...            │ ──────────    │
│   [不认识] [模糊] [认识]                  │ 快捷键         │
└──────────────────────────────────────────┴───────────────┘
```

**手机**：全屏卡片，顶部进度条，底部三按钮固定，左右滑动（左滑跳过、右滑收藏），上滑显示答案。

**词书**：内置"广东专插本考纲词汇（3000词）"，可按高频/核心/认知词分层。

**真题例句**：背单词时展示该词在历年真题中的原句。

---

### 10.14 学语法 `/learn/grammar`

**桌面**：左目录 240px + 右内容。

**目录按考点组织**：动词时态、定语从句、名词性从句、非谓语动词、虚拟语气、主谓一致等。

**内容结构**：考点精讲 → 真题示例 → 易错点辨析 → 专项练习。

**平板**：目录进抽屉。**手机**：顶部二级 Tab + 单栏内容 + 底部固定操作条。

---

### 10.15 个人中心 `/user`

**桌面**：左资料卡 280px + 右 Tab。
**平板**：顶部资料卡 + Tab。
**手机**：顶部资料卡 + 横向可滚动 Tab + 卡片流。

| Tab | 桌面 | 手机 |
|---|---|---|
| 学习统计 | 4 统计卡 + 双图并排 + 各题型得分趋势 + 各来源正确率 + 待批改数量 | 2×2 统计卡 + 图纵向 |
| 我的提交 | `n-data-table` + 状态筛选 | 卡片列表 + 顶部筛选 |
| 我的词书 | 卡片网格 3 列 | 单列卡片 |
| 错题本 | `n-data-table` + 来源筛选 + 题型筛选 | 卡片列表 + 顶部筛选 |
| 收藏 | 分组列表 | 单列 + 顶部分类 Tab |
| 设置 | 两列表单 | 单列表单 |

**设置页批改方式 UI**

```
┌──────────────────────────────────────────────────────────┐
│ 批改方式                                                  │
│                                                          │
│  ○ AI 批改                                                │
│     提交后即时出结果，含判分和解析                          │
│                                                          │
│  ○ 人工批改                                               │
│     稍后出结果，选择题优先数据库比对，作文由真人批改         │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**我的提交页**

```
┌──────────────────────────────────────────────────────────┐
│ 我的提交                                                  │
├──────────────────────────────────────────────────────────┤
│ [全部] [已批改] [部分批改] [待批改]                        │
├──────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 阅读理解 · 2026真题 · 09-17 14:32                    │ │
│ │ 状态：✅ 已批改 · 得分 8/10                          │ │
│ │ [查看详情]                                           │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 作文 · AI出题 · 09-17 10:15                          │ │
│ │ 状态：⏳ 待人工批改                                   │ │
│ │ [查看详情]                                           │ │
│ └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

提交详情经 `GET /user/submissions/:id` 获取；`graded` / `partially_graded` 的提交按题型进入对应的批改态渲染。

**错题本卡片（手机）**

```
┌──────────────────────────┐
│ 阅读理解 · 2026真题       │
│ What is the main idea?   │
│ 你的答案：C ❌            │
│ 正确答案：A ✅            │
│ [查看解析]  [重做]        │
└──────────────────────────┘
```

「重做」跳转 `/practice/reading?id=xxx&from=mistakes`（其余题型同理，见第 4 节）。

---

## 11. 数据模型

### 11.1 题目来源

```ts
type SourceType = 'real' | 'teacher' | 'ai'

interface QuestionSource {
  type: SourceType
  meta: {
    year?: number
    region?: string
    teacherName?: string
    school?: string
    model?: string
    generatedAt?: string
  }
}
```

`source` 在列表接口 `GET /practice/list?source=` 中只接受 `real` 和 `teacher`；`ai` 只用于 `POST /practice/generate`。

### 11.2 题目类型（两套：下发 / 完整）

**所有 GET 题目接口不得下发 `answer`、`explanation`、`acceptAlternatives` 字段。答案和解析只在 `POST .../submit` 返回的 `SubmitDetail` 中下发。**

下发用（ForAnswer）：

```ts
interface ReadingQuestionForAnswer {
  id: string
  type: 'reading'
  source: QuestionSource
  passage: string
  questions: {
    id: number
    stem: string
    options: { key: string; text: string }[]
  }[]
}

interface MatchingQuestionForAnswer {
  id: string
  type: 'matching'
  source: QuestionSource
  passage: string
  blanks: { id: number }[]
  options: { key: string; text: string }[]
}

interface ClozeQuestionForAnswer {
  id: string
  type: 'cloze'
  source: QuestionSource
  passage: string
  blanks: {
    id: number
    options: { key: string; text: string }[]
  }[]
}

interface GrammarFillQuestionForAnswer {
  id: string
  type: 'grammar-fill'
  source: QuestionSource
  passage: string
  blanks: {
    id: number
    hint?: string
  }[]
}

interface WritingQuestionForAnswer {
  id: string
  type: 'writing'
  source: QuestionSource
  prompt: string
  requirements: string[]
  wordLimit: number
  scoringDimensions: string[]
}
```

后端内部 / mock 存储用（Full）：

```ts
interface ReadingQuestionFull extends ReadingQuestionForAnswer {
  questions: (ReadingQuestionForAnswer['questions'][number] & {
    answer: string
    explanation: string
  })[]
}

interface MatchingQuestionFull extends MatchingQuestionForAnswer {
  blanks: { id: number; answer: string; explanation: string }[]
}

interface ClozeQuestionFull extends ClozeQuestionForAnswer {
  blanks: (ClozeQuestionForAnswer['blanks'][number] & {
    answer: string
    explanation: string
  })[]
}

interface GrammarFillQuestionFull extends GrammarFillQuestionForAnswer {
  blanks: (GrammarFillQuestionForAnswer['blanks'][number] & {
    answer: string
    acceptAlternatives?: string[]
    explanation: string
  })[]
}

interface WritingQuestionFull extends WritingQuestionForAnswer {
  sampleAnswer?: string
}
```

### 11.3 通用题目结构

```ts
type PracticeType = 'reading' | 'matching' | 'cloze' | 'grammar-fill' | 'writing'

type PracticeContentByType = {
  reading: ReadingQuestionForAnswer
  matching: MatchingQuestionForAnswer
  cloze: ClozeQuestionForAnswer
  'grammar-fill': GrammarFillQuestionForAnswer
  writing: WritingQuestionForAnswer
}

interface PracticeItem<T extends PracticeType = PracticeType> {
  id: string
  type: T
  source: QuestionSource
  difficulty: 1 | 2 | 3 | 4 | 5
  tags: string[]
  content: PracticeContentByType[T]
}
```

`content` 与 `type` 一一对应，实现端无需手动收窄类型。

### 11.4 列表项类型

```ts
interface PracticeListItem {
  id: string
  type: PracticeType
  source: QuestionSource
  title: string
  difficulty: 1 | 2 | 3 | 4 | 5
  tags: string[]
  submitted: boolean
  submissionId?: string
  submissionStatus?: GradingStatus
  score?: number
  totalScore?: number
  lastSubmittedAt?: string
}
```

### 11.5 分页响应

```ts
interface PageResult<T> {
  list: T[]
  total: number
  page: number
  size: number
}
```

### 11.6 提交结果

```ts
type GradingMethod = 'ai' | 'auto' | 'manual'
type GradingStatus = 'graded' | 'partially_graded' | 'pending'

interface SubmitResult {
  submissionId: string
  type: PracticeType
  status: GradingStatus
  gradingMethod: GradingMethod
  objectiveGraded?: boolean
  writingGraded?: boolean
  score?: number
  totalScore?: number
  correctCount?: number
  totalCount?: number
  details?: SubmitDetail[]
  estimatedTime?: string
}

interface SubmitDetail {
  id: number
  userAnswer: string
  correctAnswer: string
  isCorrect: boolean
  explanation: string
}

// 写作题（type=writing）的提交结果使用本类型
interface WritingSubmitResult extends SubmitResult {
  dimensions?: { name: string; score: number; comment: string }[]
  sampleAnswer?: string
  overallComment?: string
}
```

### 11.7 整卷结构

```ts
interface ExamPaper {
  id: string
  year: number
  title: string
  totalScore: number
  duration: number
  sections: ExamSection[]
}

interface ExamSection {
  part: number
  type: PracticeType
  score: number
  content:
    ReadingQuestionForAnswer |
    MatchingQuestionForAnswer |
    ClozeQuestionForAnswer |
    GrammarFillQuestionForAnswer |
    WritingQuestionForAnswer
}
```

答题卡编号：阅读 1-15，匹配 16-20，完形 21-35，语法 36-45，作文单独。

### 11.8 错题

```ts
interface MistakeItem {
  id: string
  questionId: string
  type: PracticeType
  source: QuestionSource
  stem: string
  userAnswer: string
  correctAnswer: string
  explanation: string
  createdAt: string
}
```

### 11.9 提交记录列表项

```ts
interface SubmissionListItem {
  id: string
  type: PracticeType
  title: string
  source: QuestionSource
  status: GradingStatus
  score?: number
  totalScore?: number
  submittedAt: string
  estimatedTime?: string
}
```

### 11.10 收藏

```ts
interface FavoriteItem {
  id: string
  type: 'word' | 'sentence'
  content: string
  refId?: string
  createdAt: string
}
```

### 11.11 单词

```ts
type WordLevel = 'high' | 'core' | 'cognitive'

interface Word {
  id: string
  spelling: string
  phonetic: string
  partOfSpeech: string
  meanings: string[]
  exampleSentence?: string
  exampleTranslation?: string
  realExamSentence?: string
  realExamSource?: string
  level: WordLevel
  audioUrl?: string
}
```

### 11.12 语法树

```ts
interface GrammarNode {
  id: string
  name: string
  children?: GrammarNode[]
}
```


---

## 12. API 约定（前端 mock 契约，MSW 实现）

**Base URL**：`/api`
**认证**：`Authorization: Bearer <token>`
**响应格式**

```json
{ "code": 0, "message": "ok", "data": {} }
```

`code !== 0` 视为错误，弹 `n-message.error`；`code === 401` 或 HTTP 401 → 清 token，跳 `/login`。

### 12.1 认证

```
POST   /auth/login
Request:  { username: string; password: string }
Response: {
  token: string
  expiresIn: number
  user: { id: string; username: string; nickname: string; avatar?: string }
}

POST   /auth/register
Request:  { username, password, nickname, email }
Response: 同 login（注册即登录）

POST   /auth/forgot-password
Request:  { email: string }
Response: 空

POST   /auth/reset-password
Request:  { token: string; newPassword: string }
Response: 空
```

### 12.2 首页

```
GET    /home/today-tasks
Response: {
  items: { type: PracticeType | 'vocab'; target: number; done: number }[]
}

GET    /home/exam-countdown
Response: { examDate: string; daysLeft: number; progress: number }

POST   /practice/smart
Request:  {}
Response: PracticeItem
```

### 12.3 刷题（5 种题型统一）

```
GET    /practice/list?type=&source=&page=&size=
Query:  source 仅接受 real | teacher
Response: PageResult<PracticeListItem>

GET    /practice/detail/:type/:id
Response: PracticeItem          # 不含答案、解析

POST   /practice/detail/:type/:id/submit
Request:  {
  answers: Record<number, string> | { content: string }
  duration: number
  gradingMethod?: 'ai' | 'manual'
}
Response: SubmitResult          # 写作题（type=writing）返回 WritingSubmitResult

POST   /practice/generate
Request:  { type: PracticeType }
Response: PracticeItem
```

> 注：大小写敏感偏好（`grammarFillCaseSensitive`）由服务端读取用户设置（见 12.7）生效，submit 请求不携带该参数。

### 12.4 真题模考

```
GET    /exam/list?year=&page=&size=
Response: PageResult<{
  id: string; year: number; title: string
  totalScore: number; duration: number
  lastScore?: number; lastSubmittedAt?: string
}>

GET    /exam/:id
Response: ExamPaper             # 不含答案、解析

POST   /exam/:id/submit
Request:  {
  objective: Record<number, string>     # 1~45，key 为答题卡编号
  writing?: { content: string }
  duration: number
  gradingMethod?: 'ai' | 'manual'
}
Response: SubmitResult
# 整卷的批改状态：
#   graded              → 完整成绩报告（GET /exam/submission/:submissionId/report）
#   partially_graded    → 客观题先出分，作文待批改，报告里 writingGraded=false
#   pending             → pending 占位页
# 语法填空判分的大小写敏感偏好由服务端读取用户设置

GET    /exam/submission/:submissionId/report
Response: {
  score: number; totalScore: number
  sections: { part: number; type: string; score: number; total: number }[]
  weakPoints: string[]
  compareWithHistory: { year: number; score: number }[]
  objectiveGraded: boolean
  writingGraded: boolean
  writingScore?: number
}
```

### 12.5 背单词

```
GET    /vocab/books
Response: { id: string; name: string; total: number; learned: number }[]

GET    /vocab/book/:bookId/words?page=&size=
Response: PageResult<Word>

POST   /vocab/review
Request:  { wordId: string; rating: 'unknown' | 'fuzzy' | 'known' }
Response: 空

GET    /vocab/task/today
Response: { newCount: number; reviewCount: number; doneCount: number; target: number }

GET    /vocab/task/due?page=&size=
Response: PageResult<Word>
```

### 12.6 学语法

```
GET    /grammar/tree
Response: GrammarNode[]

GET    /grammar/section/:id
Response: { id: string; title: string; markdown: string }
```

### 12.7 个人中心

```
GET    /user/profile
Response: {
  id: string; username: string; nickname: string; avatar?: string
  level: number; levelName: string
  continuousDays: number; totalStudyDays: number
}

GET    /user/stats
Response: {
  totalStudyTime: number
  totalWords: number
  totalReading: number
  accuracy: number
  pendingGradingCount: number
  typeAccuracy: { reading: number; matching: number; cloze: number; grammar: number; writing: number }
  scoreTrend: { date: string; reading: number; matching: number; cloze: number; grammar: number; writing: number }[]
  sourceAccuracy: { real: number; teacher: number; ai: number }
}

GET    /user/mistakes?source=&type=&page=&size=
Response: PageResult<MistakeItem>

DELETE /user/mistakes/:id

GET    /user/favorites?page=&size=
Response: PageResult<FavoriteItem>

POST   /user/favorites
Request:  { type: 'word' | 'sentence'; content: string; refId?: string }

DELETE /user/favorites/:id

GET    /user/submissions?status=&page=&size=
Query:  status: 'all' | 'graded' | 'partially_graded' | 'pending'，缺省 'all'
Response: PageResult<SubmissionListItem>

GET    /user/submissions/:id
Response: SubmitResult          # 写作题（type=writing）返回 WritingSubmitResult

GET    /user/settings
Response: {
  dailyWordGoal: number
  dailyReadingGoal: number
  accent: 'us' | 'uk'
  grammarFillCaseSensitive: boolean
  gradingMethod: 'ai' | 'manual'
  reminderTime?: string
}

PUT    /user/settings
Request: 同上字段（可选，未提供的字段保持不变）
Response: 空
```

> 说明：
> - `themeMode` 不在本接口：主题偏好只存 localStorage（见 5.3），不做服务端存储。
> - `gradingMethod`、`grammarFillCaseSensitive` 为**服务端生效的判分偏好**：服务端在批改 / 判分时读取用户设置，前端 submit 请求不重复携带。

### 12.8 路径歧义规避

静态段先于动态段声明。以下路径已避开冲突：

- `POST /practice/generate` 不与 `/practice/detail/:type/:id` 冲突
- `GET /practice/detail/:type/:id` 用 `detail` 段隔离
- `GET /vocab/task/today`、`GET /vocab/task/due` 用 `task` 段隔离
- `GET /vocab/book/:bookId/words`
- `GET /exam/list` **必须**先于 `GET /exam/:id` 匹配（MSW 里静态段在前）

---

## 13. Mock 方案（MSW）

### 13.1 启用方式

**一次性生成 worker 脚本**：`npx msw init public/`（产出 `public/mockServiceWorker.js`，勿手写）。

`.env.development`：

```
VITE_USE_MOCK=true
```

`src/env.d.ts` 声明自定义环境变量类型：

```ts
interface ImportMetaEnv {
  readonly VITE_USE_MOCK: string
}
```

`main.ts`：

```ts
if (import.meta.env.VITE_USE_MOCK === 'true') {
  const { worker } = await import('@/mocks/browser')
  await worker.start({ onUnhandledRequest: 'bypass' })
}
```

> 注：顶层 `await` 需 tsconfig `target` / `module` 支持（ES2022，Vite 默认 esnext 即可）。

**MSW 与 PWA 的 Service Worker 互斥**：同一站点同一 scope 只有最后一个注册的 SW 生效，两个 SW 不能同时启用。约定：

- 开发 / 演示一律走 dev server：MSW 生效，vite-plugin-pwa 的 `devOptions.enabled` 保持默认 `false`；
- 生产构建：包含 PWA 的 SW，但不打包、不注册 MSW（`VITE_USE_MOCK` 仅存在于 `.env.development`，生产构建取值为 undefined，注册代码自动跳过）。

### 13.2 数据存放

`src/mocks/data/` 存 **Full 版** 数据（含答案）。handler 返回前过滤掉答案字段。

### 13.3 Handler 编写约定

- 每个模块一个 handler 文件
- 统一从 `mocks/data/` 取数据
- GET 题目接口必须过滤答案字段
- submit 接口按 `gradingMethod` 和 `status` 返回不同 `SubmitResult`
- 模拟延时 200~500ms

### 13.4 过滤工具

```ts
// src/mocks/utils/stripAnswer.ts
export function stripReadingAnswer(q: ReadingQuestionFull): ReadingQuestionForAnswer {
  return {
    ...q,
    questions: q.questions.map(({ answer, explanation, ...rest }) => rest),
  }
}
// 其余题型同理
```

---

## 14. 组件清单

| 组件 | 职责 | 三端差异 |
|---|---|---|
| `AppLogo` | Logo | 三端一致 |
| `AppLayout` | 主框架 | 按 `useDevice` 切换 |
| `TopNav` | 顶部一级 | 仅桌面/平板 |
| `SideNav` | 左侧二级 | 桌面常驻 / 平板抽屉复用 |
| `NavDrawer` | 平板抽屉容器 | 仅平板 |
| `MobileTopBar` | 手机顶部 | 仅手机 |
| `MobileSubTabs` | 手机二级 | 仅手机 |
| `MobileTabBar` | 手机底部 | 仅手机 |
| `ThemeToggle` | 主题切换按钮 | 桌面/平板顶栏 |
| `SourceSwitcher` | 来源切换 | 三端一致 |
| `PageHeader` | 页面标题+操作 | 三端自适应 |
| `StatCard` | 统计卡 | 三端一致 |
| `EmptyState` | 空状态 | 三端一致 |
| `LoadingState` | 加载状态 | 三端一致 |
| `PendingView` | 待批改占位页 | 三端一致 |
| `CountdownTimer` | 计时 | 三端一致 |
| `WordCard` | 单词卡 | 桌面中卡 / 手机全屏 |
| `WordActionBar` | 三按钮 | 桌面内嵌 / 手机底部固定 |
| `WordProgressAside` | 背单词进度栏 | 桌面右侧 / 手机折叠到顶 |
| `WordListTable` | 单词列表 | 桌面表格 / 手机卡片 |
| `GrammarTree` | 语法目录 | 桌面侧栏 / 移动抽屉 |
| `GrammarContent` | 语法内容 | 三端一致 |
| `PassageRenderer` | 文章渲染 | 点词查词三端一致 |
| `QuestionList` | 题目列表 | 三端一致 |
| `QuestionItem` | 单选题 | 三端一致 |
| `AnswerSheet` | 答题卡 | 三端一致 |
| `OptionPool` | 五选五选项池 | 三端一致，点选 |
| `OptionCard` | 五选五选项卡 | 三端一致 |
| `BlankSlot` | 五选五空格槽位 | 三端一致，点选 |
| `OptionPickerDrawer` | 手机选项选择面板 | 仅手机 |
| `ClozePassage` | 完形文章 | 三端一致 |
| `ClozeQuestion` | 完形题目 | 三端一致 |
| `FillPassage` | 语法填空文章 | 三端一致 |
| `FillBlank` | 语法填空空位 | 三端一致 |
| `WritingPrompt` | 作文题目 | 三端一致 |
| `WritingEditor` | 作文编辑 | 三端一致 |
| `PracticeListPage` | 真题/模拟题列表（复用） | 三端自适应 |
| `PracticeSourceBar` | 来源切换栏（复用） | 三端一致 |
| `ExamPaper` | 整卷答题 | 三端一致 |
| `ExamReport` | 成绩报告 | 三端一致 |

---

## 15. 开发顺序

1. 初始化项目（Vite + Vue3 + TS + Naive UI + Router + Pinia + VueUse + ESLint + Prettier + vite-plugin-pwa）
2. 集成 MSW：`npx msw init public/` 生成 worker 脚本 + 编写 mock handlers（先 mock auth，再逐模块补）
3. 全局样式、双主题变量、主题覆盖、`useDevice`、`useImmersive`、`useHighlightTheme`、`usePlaceholder`
4. `AppLayout` + 三端导航壳
5. 路由 + 空页面占位，跑通三端切换 + 主题切换
6. 登录页 + 注册页 + 忘记密码 + 重置密码 + 用户 store + 请求封装（含 401 拦截）
7. 首页 Dashboard（倒计时 + 今日任务 + 题型得分率 + 智能练习 + 待批改提示）
8. 阅读理解（沉浸 + 点词查词 + 批改态/pending 态 + 来源切换）
9. 五选五（三端点选 + 两种选择顺序）
10. 完形填空
11. 语法填空
12. 作文
13. 真题模考（整卷 + 成绩报告 + 部分批改态）
14. 背单词
15. 学语法
16. 个人中心（统计 + 我的提交 + 错题 + 词书 + 收藏 + 设置含批改方式）
17. PWA、安全区、手势、性能优化
18. 移动端适配清单逐项检查
19. 双主题全站走查

---

## 16. 移动端适配清单

- [ ] 所有可点击元素 ≥ 44×44px
- [ ] 底部 Tab 加 `env(safe-area-inset-bottom)`
- [ ] 顶部栏加 `env(safe-area-inset-top)`
- [ ] 输入框 font-size ≥ 16px
- [ ] 正文 ≥ 15px，禁止 < 12px
- [ ] 表格改卡片流
- [ ] 弹窗优先 `n-drawer placement="bottom"`
- [ ] `touch-action: manipulation` 禁用双击缩放
- [ ] 横屏阅读页可锁竖屏
- [ ] 长列表虚拟滚动或分页
- [ ] PWA manifest + 图标 + 启动图 + hljs CSS 预缓存

---

## 17. 验收标准

1. 三种断点下布局正确切换，无横向滚动条
2. 桌面：顶部一级 + 左侧二级常驻
3. 平板：顶部一级 + 左侧抽屉
4. 手机：底部一级 Tab + 顶部二级 Tab
5. 所有答题页三端均进入沉浸模式
6. 5 种题型页面均支持来源切换：真题 / 模拟题 / AI出题
7. AI出题点击后直接生成，无参数面板，loading 后进入答题
8. 五选五三端均为点选，支持两种选择顺序，已填可移除
9. 阅读节点词查词三端可用
10. 真题模考可完整答题、交卷、生成成绩报告
11. 首页显示倒计时、今日任务、各题型得分率
12. 错题本支持来源和题型筛选，支持删除、重做
13. 收藏支持增删
14. 亮色为亮蓝色调，暗色为暗蓝色调
15. 刷新后主题保持，首次访问跟随系统
16. 设置页可切换：跟随系统 / 亮色 / 暗色
17. 三端主题切换入口可用
18. 暗色下无白底闪烁，无对比度过低的文字
19. 图表、Markdown 代码块、批改态颜色均正确适配暗色
20. highlight.js 双主题切换正常（本地打包，PWA 离线可用）
21. 占位符统一为 `[[n]]`
22. 所有列表接口返回统一 `PageResult`
23. 所有 submit 接口返回统一 `SubmitResult`（写作题为 `WritingSubmitResult`）
24. 登录 401 自动跳转登录页
25. 设置页可切换批改方式：AI 批改 / 人工批改
26. 提交响应包含 `status` 和 `gradingMethod`
27. `status === 'pending'` 时，答题页展示待批改占位页，不展示对错和解析
28. `status === 'graded'` 时，正常进入批改态
29. `status === 'partially_graded'` 时，客观题先出分，作文显示待批改
30. 作文在人工批改模式下提交后走 pending 态
31. 首页显示待批改数量提示（数量 > 0 时），「查看」进入我的提交页待批改筛选
32. 用户离开后重新进入 pending 题目，可查询最新状态
33. 个人中心有「我的提交」页，可按状态筛选（全部 / 已批改 / 部分批改 / 待批改）
34. 登录 / 注册 / 忘记密码 / 重置密码页面可用
35. **所有 GET 题目接口不下发答案和解析字段**
36. MSW mock 生效，无后端也能完整跑通所有页面
37. 开发环境 MSW 与 PWA Service Worker 不同时启用
38. Lighthouse 移动端 Performance ≥ 80
39. 无控制台报错
40. 所有接口错误有 `n-message` 提示

---

## 18. 交付物

- 完整可运行的前端项目（根目录即 `LinguaCraft`）
- README：启动方式、环境变量、构建命令、mock 开关说明
- 关键页面三端截图（亮色 + 暗色各一套）

---

## 附录：v6 修订记录（相对 v5）

1. 补注册页：目录树 `Register.vue`、路由 `/register`、页面设计 10.2、开发顺序与验收标准（34）。
2. `themeMode` 从 `/user/settings` 移除：主题偏好权威源仅 localStorage（5.3 / 12.7 已同步）。
3. 明确 `gradingMethod`、`grammarFillCaseSensitive` 为服务端生效的判分偏好，submit 请求不携带（9.3 / 10.10 / 12.3 / 12.4 / 12.7）。
4. 明确 MSW 与 PWA Service Worker 互斥及运行形态（13.1 + 验收 37）。
5. 补 `public/mockServiceWorker.js`（`npx msw init public/` 生成）与 `src/env.d.ts` 声明（第 3 节 + 13.1 + 开发顺序第 2 步）。
6. 写作题 submit / 提交详情响应注明返回 `WritingSubmitResult`（12.3 / 12.7 / 11.6 注释）。
7. `src/mocks/utils/stripAnswer.ts` 纳入目录树，13.4 路径注释同步。
8. `gradingMethod` 提示文案改为 status × gradingMethod 二维表（9.3）。
9. `PracticeItem` 改为 `content` 与 `type` 一一对应的映射类型（11.3）。
10. 首页待批改提示「查看」目标明确为 `/user/submissions?status=pending`（10.5 + 验收 31）。
11. `/user/submissions` 的 `status` 枚举补充（12.7），我的提交页筛选栏同步（10.15 + 验收 33）。
12. 13.1 补充顶层 `await` 的 tsconfig 要求。
13. reset-password 的 URL token 泄漏风险备注（10.4）。

---

**任务书 v6 完。**